const RINCHAN_V071 = 'v0.9.23';

function v071ReadJson(key, fallback) { try { const raw = localStorage.getItem(key); return raw ? JSON.parse(raw) : fallback; } catch (e) { return fallback; } }
function v071SaveJson(key, value) { localStorage.setItem(key, JSON.stringify(value)); }
function v071Num(n) { return Number(n || 0).toLocaleString('ja-JP'); }
function v071Today() { const d = new Date(); return d.getFullYear() + '-' + String(d.getMonth()+1).padStart(2,'0') + '-' + String(d.getDate()).padStart(2,'0'); }
function v098EscapeHtml(value) { return String(value || '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])); }

function v071Level(count, steps) {
  if (count >= 100 || steps >= 500000) return 7;
  if (count >= 60 || steps >= 300000) return 6;
  if (count >= 40 || steps >= 200000) return 5;
  if (count >= 25 || steps >= 100000) return 4;
  if (count >= 10 || steps >= 50000) return 3;
  if (count >= 3 || steps >= 10000) return 2;
  return 1;
}
function v071Tree(level) { return ['', '🌱', '🌿', '🌳', '🌸🌳', '🌳🐦', '🌳🦋', '🌳✨'][level] || '🌱'; }
function v071DisplayName(user) { return String((user && (user.name || user.nick || user.employeeId || user.id)) || 'ゲスト'); }
function v071PersonId(user) { return String((user && (user.employeeId || user.id || user.participantId || user.name || user.nick)) || 'tree').trim().replace(/\s+/g,'-').slice(0,90) || 'tree'; }
function v071PersonLabel(user) { const name=String((user&&(user.name||''))||'').trim(); const nick=String((user&&(user.nick||''))||'').trim(); const emp=String((user&&(user.employeeId||user.id||''))||'').trim(); if(name&&nick&&name!==nick)return name+'（'+nick+'）'; if(name)return name; if(nick&&emp)return nick+'（社員番号 '+emp+'）'; if(nick)return nick; if(emp)return '社員番号 '+emp; return 'ゲスト'; }
function v071PersonSubLabel(user,groupLabel) { const dept=String((user&&(user.dept||''))||groupLabel||'').trim(); const emp=String((user&&(user.employeeId||user.id||''))||'').trim(); const parts=[]; if(dept)parts.push(dept); if(emp&&!String(emp).startsWith('sample-'))parts.push('社員番号 '+emp); parts.push(v071Num(user&&user.totalSteps)+'歩 / '+v071Num(user&&user.activityCount)+'回'); return parts.join(' ・ '); }
function v071SenderLabel(user) { const name=String((user&&(user.name||''))||'').trim(); const nick=String((user&&(user.nick||''))||'').trim(); const emp=String((user&&(user.employeeId||user.id||''))||'').trim(); if(name&&nick&&name!==nick)return name+'（'+nick+'）'; if(name)return name; if(nick)return nick; if(emp)return '社員番号 '+emp; return 'どなたか'; }

function v071Area(dept) {
  const d = String(dept || '');
  if (d.includes('看護')) return { key:'nurse', x:22, y:34, label:'看護部', area:'看護部エリア' };
  if (d.includes('リハ')) return { key:'reha', x:70, y:34, label:'リハビリテーション部', area:'リハビリエリア' };
  if (d.includes('介護')) return { key:'care', x:76, y:66, label:'介護部', area:'介護部エリア' };
  if (d.includes('事務')) return { key:'office', x:34, y:68, label:'事務部', area:'事務部エリア' };
  if (d.includes('医')) return { key:'doctor', x:50, y:50, label:'医局', area:'中央エリア' };
  return { key:'other', x:50, y:52, label:d || 'その他', area:'中央エリア' };
}
function v097Season(){const m=new Date().getMonth()+1;if(m>=3&&m<=5)return{key:'spring',name:'春',icon:'🌸',items:['🌸','🌷','🦋'],msg:'春の杜に、やさしい花が咲いています。'};if(m>=6&&m<=8)return{key:'summer',name:'夏',icon:'🌿',items:['🌻','🌿','🎐'],msg:'夏の杜が、青々と育っています。'};if(m>=9&&m<=11)return{key:'autumn',name:'秋',icon:'🍁',items:['🍁','🍄','🌰'],msg:'秋の杜に、実りの気配が広がっています。'};return{key:'winter',name:'冬',icon:'❄️',items:['❄️','✨','☃️'],msg:'冬の杜に、静かな光が灯っています。'}}
function v097Time(){const h=new Date().getHours();if(h<6)return{key:'night',name:'夜',sky:'🌙',msg:'夜の杜は静かに休んでいます。'};if(h<11)return{key:'morning',name:'朝',sky:'☀️',msg:'朝の光が杜に差し込んでいます。'};if(h<17)return{key:'day',name:'昼',sky:'🌤️',msg:'昼の杜に、明るい風が通っています。'};if(h<20)return{key:'evening',name:'夕方',sky:'🌇',msg:'夕方の杜が、少しあたたかい色になりました。'};return{key:'night',name:'夜',sky:'🌙',msg:'夜の杜は静かに休んでいます。'}}
function v098GetDashboardMembers(){const cached=v071ReadJson('rinchanDashboardCache',null);if(cached&&cached.data&&Array.isArray(cached.data.members)&&cached.data.members.length)return cached.data.members;return null;}

document.addEventListener('DOMContentLoaded',()=>{renderV071Map();});

function renderV071Map(){
  const map=document.getElementById('moriMap'); if(!map)return;
  const season=v097Season(); const time=v097Time();
  document.body.classList.add('mori-season-'+season.key,'mori-time-'+time.key);
  const note=document.getElementById('moriUpdatedAt'); if(note)note.textContent=season.icon+' '+season.name+'の杜・'+time.name;
  const msg=document.getElementById('moriSeasonMessage'); if(msg)msg.textContent=season.msg;
  const ht=document.getElementById('moriHighlightTitle'); if(ht)ht.textContent=season.icon+' '+season.name+'の見どころ';
  const hx=document.getElementById('moriHighlightText'); if(hx)hx.textContent=time.msg+' 今日も無理せず、いこうね。';
  const users=(v071ReadJson('rinchanMoriMembers',null)||v098GetDashboardMembers()||buildLocalMembersV071()).slice();
  const groups=buildDeptGroupsV113(users).sort((a,b)=>b.totalSteps-a.totalSteps);
  map.innerHTML='<span class="map-bg sun">'+time.sky+'</span><span class="map-bg cloud-a">☁️</span><span class="map-bg cloud-b">☁️</span><span class="map-bg bench">🪑</span><span class="map-bg flower">'+season.items[0]+'</span><span class="map-bg seasonal-a">'+season.items[1]+'</span><span class="map-bg seasonal-b">'+season.items[2]+'</span><span class="rinchan-walker" id="rinchanWalker">😊</span><div class="dept-area area-nurse">看護部</div><div class="dept-area area-reha">リハビリ</div><div class="dept-area area-care">介護部</div><div class="dept-area area-office">事務部</div>';
  groups.slice(0,8).forEach((group,i)=>{const level=v071Level(group.activityCount,group.totalSteps);const btn=document.createElement('button');btn.type='button';btn.className='mori-tree-node dept-node level-'+level+(i<3?' top-tree rank-'+(i+1):'');btn.style.left=group.x+'%';btn.style.top=group.y+'%';group._x=group.x;group._y=group.y;btn.innerHTML='<span>'+v071Tree(level)+'</span><small>'+v098EscapeHtml(group.label)+'<br>'+group.members.length+'人</small>';btn.addEventListener('click',()=>openDeptCardV113(group,level,i+1));map.appendChild(btn);});
}
function buildDeptGroupsV113(users){const map={};users.forEach(user=>{const area=v071Area(user.dept);const key=area.key+'-'+area.label;if(!map[key])map[key]={id:key,key:area.key,label:area.label,area:area.area,x:area.x,y:area.y,members:[],activityCount:0,totalSteps:0};map[key].members.push(user);map[key].activityCount+=Number(user.activityCount||0);map[key].totalSteps+=Number(user.totalSteps||0);});return Object.keys(map).map(key=>{const group=map[key];group.members=group.members.slice().sort((a,b)=>Number(b.totalSteps||0)-Number(a.totalSteps||0));return group;});}
function buildLocalMembersV071(){const p=v071ReadJson('rinchanParticipant',{})||{};const acts=v071ReadJson('rinchanActivities',[]);const total=acts.reduce((s,a)=>s+Number(a.steps||0),0);const me={id:p.id||p.employeeId||'me',employeeId:p.employeeId||p.id||'me',name:p.name||p.nick||'あなた',nick:p.nick||'',dept:p.dept||'未設定',activityCount:acts.length,totalSteps:total};const samples=[{id:'sample-nurse',name:'看護部の木',dept:'看護部',activityCount:18,totalSteps:82000},{id:'sample-reha',name:'リハ部の木',dept:'リハビリテーション部',activityCount:32,totalSteps:142000},{id:'sample-office',name:'事務部の木',dept:'事務部',activityCount:8,totalSteps:36000},{id:'sample-care',name:'介護部の木',dept:'介護部',activityCount:14,totalSteps:62000},{id:'sample-doctor',name:'医局の木',dept:'医局',activityCount:7,totalSteps:28000},{id:'sample-nutrition',name:'栄養科の木',dept:'栄養科',activityCount:11,totalSteps:47000}];return [me].concat(samples);}
function openDeptCardV113(group,level,rank){
  window.rinchanSelectedTree=group;const card=document.getElementById('treeInfoCard');if(!card)return;card.classList.remove('hidden');const rankText=rank&&rank<=3?' / 🏆 部署ランキング '+rank+'位':'';const members=group.members.slice(0,12);
  card.innerHTML='<button class="tree-card-close" type="button" aria-label="閉じる">×</button><p class="label">部署・グループの木'+rankText+'</p><div class="tree-card-icon dept-icon">'+v071Tree(level)+'</div><h2 class="dept-card-title">'+v098EscapeHtml(group.label)+'</h2><p>'+v098EscapeHtml(group.area)+'・'+group.members.length+'人</p><div class="mini-stats"><div><strong>Lv.'+level+'</strong><small>木レベル</small></div><div><strong>'+v071Num(group.totalSteps)+'</strong><small>歩</small></div><div><strong>'+v071Num(group.activityCount)+'</strong><small>記録</small></div></div><p class="dept-note">ありがとうを送る相手を間違えないように、氏名・ニックネーム・所属を表示します。</p><div class="dept-member-list" id="deptMemberList"></div>';
  card.querySelector('.tree-card-close').addEventListener('click',closeTreeCardV071);const list=card.querySelector('#deptMemberList');
  list.innerHTML=members.map(member=>{const id=v071PersonId(member);const already=hasThanksTodayV077(id);const label=v071PersonLabel(member);const meta=v071PersonSubLabel(member,group.label);return '<div class="dept-member-item"><div><strong class="dept-member-name">'+v098EscapeHtml(label)+'</strong><small class="dept-member-meta">'+v098EscapeHtml(meta)+'</small></div><button type="button" class="dept-member-thanks" data-thanks-id="'+v098EscapeHtml(id)+'" data-name="'+v098EscapeHtml(label)+'" data-dept="'+v098EscapeHtml(member.dept||group.label)+'"'+(already?' disabled':'')+'>'+(already?'送信済み':'ありがとう')+'</button></div>';}).join('');
  list.querySelectorAll('.dept-member-thanks').forEach(btn=>btn.addEventListener('click',()=>openThanksSheetV096(btn.dataset.thanksId,btn.dataset.name,btn.dataset.dept)));card.scrollIntoView({behavior:'smooth',block:'center'});
}
function closeTreeCardV071(){const card=document.getElementById('treeInfoCard');if(card)card.classList.add('hidden');}
function getThanksCountV073(id){const data=v071ReadJson('rinchanThanks',{});return Number(data[id]||0);}
function hasThanksTodayV077(id){const log=v071ReadJson('rinchanThanksDaily',{});return log[id]===v071Today();}
function openThanksSheetV096(id,targetName,targetDept){if(hasThanksTodayV077(id)){alert('この方へのありがとうは今日は送信済みです。');return;}window.rinchanPendingThanksId=id;window.rinchanThanksReason='';window.rinchanThanksTargetName=targetName||'';window.rinchanThanksTargetDept=targetDept||'';const old=document.getElementById('thanksSheetLayer');if(old)old.remove();const layer=document.createElement('div');layer.id='thanksSheetLayer';layer.className='thanks-sheet-layer';layer.innerHTML='<div class="thanks-sheet" role="dialog" aria-modal="true" aria-label="ありがとう確認"><button class="tree-card-close thanks-sheet-close" type="button" aria-label="閉じる">×</button><div class="thanks-sheet-heart">❤️</div><h2>ありがとうを届けますか？</h2><strong class="thanks-sheet-target">'+v098EscapeHtml(targetName||'選択した方')+'さんへ</strong><p>送る相手を確認してから届けてください。<br><small>理由は選ばなくても届けられます。</small></p><div class="thanks-reason-options sheet-options"><button type="button" data-reason="元気をもらった">😊 元気をもらった</button><button type="button" data-reason="助けてもらった">🤝 助けてもらった</button><button type="button" data-reason="頑張っていた">🌸 頑張っていた</button></div><button class="submit pill-button thanks-button thanks-confirm-button" type="button">❤️ ありがとうを届ける</button><p class="thanks-privacy-note">通信には個人名ではなく、部署単位の出来事として表示します。本人のマイページには送り主が表示されます。</p></div>';document.body.appendChild(layer);layer.querySelector('.thanks-sheet-close').addEventListener('click',closeThanksSheetV096);layer.addEventListener('click',event=>{if(event.target===layer)closeThanksSheetV096();});layer.querySelectorAll('.thanks-reason-options button').forEach(button=>button.addEventListener('click',()=>selectThanksReasonV095(button,button.dataset.reason||'')));layer.querySelector('.thanks-confirm-button').addEventListener('click',confirmThanksV096);}
function closeThanksSheetV096(){const layer=document.getElementById('thanksSheetLayer');if(layer)layer.remove();}
function selectThanksReasonV095(btn,reason){window.rinchanThanksReason=reason;document.querySelectorAll('.thanks-reason-options button').forEach(b=>b.classList.remove('selected'));if(btn)btn.classList.add('selected');}
function confirmThanksV096(){const id=window.rinchanPendingThanksId;if(!id)return;sendThanksV073(id);closeThanksSheetV096();showThanksDeliveredV096();}
function showThanksDeliveredV096(){const msg=document.createElement('div');msg.className='thanks-delivered-toast';msg.innerHTML='<span>🦋</span><strong>ありがとうが届きました♪</strong>';document.body.appendChild(msg);setTimeout(()=>msg.remove(),1800);}
function sendThanksV073(id){if(hasThanksTodayV077(id)){alert('この方へのありがとうは今日は送信済みです。');return;}const data=v071ReadJson('rinchanThanks',{});data[id]=Number(data[id]||0)+1;v071SaveJson('rinchanThanks',data);const daily=v071ReadJson('rinchanThanksDaily',{});daily[id]=v071Today();v071SaveJson('rinchanThanksDaily',daily);const reason=window.rinchanThanksReason||'ありがとう';const reasons=v071ReadJson('rinchanThanksReasons',{});reasons[reason]=Number(reasons[reason]||0)+1;v071SaveJson('rinchanThanksReasons',reasons);document.querySelectorAll('[data-thanks-id="'+CSS.escape(id)+'"]').forEach(btn=>{btn.textContent='送信済み';btn.disabled=true;});addThanksTimelineV090(window.rinchanThanksTargetDept,reason);saveThanksRemoteV113(id,reason);runRinchanDeliveryV074(window.rinchanSelectedTree);runThanksHeartV073();}
function addThanksTimelineV090(targetDept,reason){const list=v071ReadJson('rinchanGoodTimeline',[]);const dept=targetDept||'杜の仲間';const suffix=reason&&reason!=='ありがとう'?'「'+reason+'」のありがとうです。':'応援の気持ちが届きました。';list.unshift({id:'T'+Date.now().toString(36),icon:'❤️',title:'ありがとうが届きました',body:dept+'の仲間に、'+suffix,publicBody:dept+'の仲間に、'+suffix,targetDept:dept,createdAt:new Date().toISOString()});v071SaveJson('rinchanGoodTimeline',list.slice(0,50));}
function saveThanksRemoteV113(id,reason){const p=v071ReadJson('rinchanParticipant',{})||{};const payload={thanksId:'K'+Date.now().toString(36)+Math.random().toString(36).slice(2,8),fromParticipantId:p.employeeId||p.id||'',fromName:v071SenderLabel(p),toParticipantId:id,toName:window.rinchanThanksTargetName||'',toDept:window.rinchanThanksTargetDept||'',reason:reason||'ありがとう',createdAt:new Date().toISOString(),version:RINCHAN_V071};if(typeof rinchanApi==='function')rinchanApi('saveThanks',payload).then(result=>{if((!result||!result.ok)&&typeof queuePending==='function')queuePending('saveThanks',payload,(result&&(result.reason||result.error))||'remote_error');}).catch(error=>{if(typeof queuePending==='function')queuePending('saveThanks',payload,error&&error.message?error.message:'remote_error');});}
function runRinchanDeliveryV074(tree){const map=document.getElementById('moriMap');const r=document.getElementById('rinchanWalker');if(!map||!r||!tree)return;r.classList.add('walking');r.style.left='8%';r.style.top='82%';setTimeout(()=>{r.style.left=(tree._x||50)+'%';r.style.top=(tree._y||50)+'%';},80);setTimeout(()=>{const h=document.createElement('span');h.className='delivered-heart';h.textContent='❤️';h.style.left=(tree._x||50)+'%';h.style.top=(tree._y||50)+'%';map.appendChild(h);setTimeout(()=>h.remove(),1500);},1100);setTimeout(()=>r.classList.remove('walking'),1600);}
function runThanksHeartV073(){const layer=document.createElement('div');layer.className='thanks-heart-layer';['❤️','💚','💛','❤️','✨'].forEach((h,i)=>{const s=document.createElement('span');s.textContent=h;s.style.left=(20+i*14)+'%';s.style.animationDelay=(i*.08)+'s';layer.appendChild(s);});document.body.appendChild(layer);setTimeout(()=>layer.remove(),1800);}
