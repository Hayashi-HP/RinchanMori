# HTML_ASSET_AUDIT

## 目的

Step2 Cleanup1 の最初の作業として、各HTMLが読み込んでいるCSS / JSを棚卸しします。
ここではまだ削除・統合は行いません。

## 基本方針

- 画面を壊さない
- まず現状を見える化する
- 読み込み順を変えない
- 統合候補は別途判断する

---

## 対象画面

| 画面 | ファイル | 状態 |
|---|---|---|
| ホーム | `index.html` | 監査対象 |
| 杜 | `pages/mori.html` | 監査対象 |
| 歩数記録 | `pages/activity.html` | 監査対象 |
| 通信 | `pages/news.html` | 監査対象 |
| マイページ | `pages/mypage.html` | 監査対象 |
| ログイン | `pages/login.html` | 監査済み |
| 新規登録 | `pages/register.html` | 監査済み |
| 管理 | `pages/admin.html` | 監査済み |

---

## 現時点の確認済み傾向

### ホーム

- Core CSSに加えて、ホーム専用、季節、花通知、PC表示修正などを読み込む
- キャッシュ番号は主に `1141`
- フッターナビは復旧済み

### 杜

- 杜専用CSS、World系CSS、スマホ装飾修正、Phase2世界エンジンを読み込む
- キャッシュ番号は主に `1200`
- Phase2の中心画面

### 歩数記録

- 比較的古いキャッシュ番号 `1118`
- 表示は安定しているが、現在基準との差が大きい

### 通信

- キャッシュ番号は主に `1137`
- お知らせ、今日のまとめ、ありがとうの広がりを表示
- ありがとう受け取りはマイページに集約済み

### マイページ

- キャッシュ番号は主に `1142`
- ありがとう履歴、花受け取り、既読同期の中心画面

### ログイン

- キャッシュ番号は主に `1117`
- CSSは軽量
- JSはCore + Auth中心
- 画面構成はシンプル
- 今すぐ変更しない

### 新規登録

- キャッシュ番号は主に `1137`
- ログインと近いCSS構成
- `v127-departments.js` を読み込み、部署リストに対応
- 画面構成はシンプル
- 今すぐ変更しない

### 管理画面

- キャッシュ番号は主に `1034`
- 管理専用CSSを複数読み込み
- 表示ラベルが `v1.0.34` のまま
- 現在バージョンとの差が大きい
- 今後、管理画面の整理対象として優先度高め

---

## 監査で確認すること

1. CSSの読み込み数
2. JSの読み込み数
3. Core系が共通で読み込まれているか
4. 画面専用CSSが適切か
5. HotFixが過剰でないか
6. キャッシュ番号が不自然でないか
7. インラインCSS / JSが多すぎないか

---

## 初期判断

### すぐ変更しない

- ホーム
- 杜
- マイページ
- 通信
- 歩数記録
- ログイン
- 新規登録

理由：現在の表示安定性を優先するため。

### 優先的に整理候補

- 管理画面

理由：キャッシュ番号と画面内表示ラベルが古く、現在バージョンとの差が大きいため。

---

## Cleanup1 のゴール

- 各HTMLの読み込み構成を把握する
- 統合候補を安全に分類する
- 次のCleanup2で触る対象を決める

## 注意

このファイルは削除対象リストではありません。
HTML読み込みを整理する前の安全確認リストです。
