# DECISIONS

## 目的

このファイルは、りんちゃんの杜の重要な設計判断を記録します。

「なぜこの仕様にしたのか」を残すことで、将来の保守・改修時に迷わないようにします。

---

## D001 開発指示を一本化する

### 判断

今後の開発指示は、このチャットに一本化する。

### 理由

PCとスマホの別チャットで同時にGitHubへ更新すると、READMEやHTMLなどの更新が競合し、内容が古く戻る危険があるため。

### 影響

- README / CHANGELOG / ROADMAP の整合性を保ちやすくなる
- 実装の順番が明確になる
- バージョン管理が安定する

---

## D002 ありがとうの花受け取りはマイページに集約する

### 判断

ありがとうの花を受け取る機能は、通信ではなくマイページに集約する。

### 理由

通信とマイページの両方に受け取り機能があると、利用者がどこで操作すべきか迷うため。

### 影響

- ホーム通知はマイページの `#thanks` へ誘導する
- 通信は全体向けの「ありがとうの広がり」に限定する
- 個人宛の受け取り操作はマイページだけにする

---

## D003 ありがとう送信時だけ本名を表示する

### 判断

通常表示はニックネーム優先とし、ありがとう送信時だけ本名を補助表示する。

### 理由

普段は親しみやすさとプライバシーを優先する一方、ありがとう送信時は相手を間違えないことが重要なため。

### 影響

- 杜マップはニックネーム中心
- 送信確認UIではニックネーム、本名、部署を表示
- 送信前に確認ダイアログを表示する

---

## D004 自分が送ったありがとうは自分宛の花に出さない

### 判断

受け取れる花は、`toParticipantId == 自分` かつ `fromParticipantId != 自分` のものだけにする。

### 理由

送信者本人に「花が届いた」と表示されると意味が逆転し、混乱するため。

### 影響

- 自分が送ったありがとうは履歴側に表示
- 自分宛の未受取花には表示しない
- ホーム通知にも表示しない

---

## D005 既読・受け取り状態は `user_reads` を正とする

### 判断

お知らせ既読、ありがとう花受け取り状態は、端末内保存ではなく `user_reads` を中心に管理する。

### 理由

PCとスマホで既読・未読件数が異なる問題を解消するため。

### 影響

- `readNewsIds` をサーバー保存
- `readThanksFlowerIds` をサーバー保存
- 同期後に各画面を再描画する

---

## D006 HotFixはすぐ削除しない

### 判断

古いHotFix CSS / JSはすぐ削除せず、監査後に段階的に統合する。

### 理由

現在の表示安定性は複数のHotFixに支えられており、削除するとスマホ・PCの表示崩れが再発する可能性があるため。

### 影響

- `HOTFIX_AUDIT.md` で分類する
- Cleanupフェーズで安全に統合する
- 1回につき1カテゴリのみ整理する

---

## D007 キャッシュ番号は一括統一しない

### 判断

現時点で全画面のキャッシュ番号を一括で `1200` に統一しない。

### 理由

画面ごとの安定状態が異なり、一括更新すると未検証の表示崩れが発生する可能性があるため。

### 影響

- 修正した画面だけキャッシュ番号を更新する
- Cleanupフェーズで全画面統一を検討する
- `CACHE_AUDIT.md` で管理する

---

## D008 りんちゃんの顔は崩さない

### 判断

りんちゃんの表情や姿を大きく変える画像生成・加工は避ける。

### 理由

キャラクターの同一性が崩れると、利用者が違和感を持つため。

### 影響

- りんちゃん本体は原則固定
- 世界観の変化は周囲の雲、花、動物、季節演出で表現する
- 動きを入れる場合も顔を崩さない範囲に限定する

---

## D009 Phase2は「生きた杜」を優先する

### 判断

Phase2ではランキングより先に、杜の世界観・動き・季節感を優先する。

### 理由

このアプリは単なる歩数ランキングではなく、毎日開きたくなる温かい院内健康アプリを目指すため。

### 影響

- v1.2.0で杜世界エンジンを追加
- 次は生き物・成長演出・杜ニュースへ進む
- ランキングやバッジはPhase3で本格化する

---

## D010 正式ドキュメント体系を採用する

### 判断

以下の文書を正式ドキュメントとして管理する。

- README.md
- CHANGELOG.md
- ROADMAP.md
- ARCHITECTURE.md
- HOTFIX_AUDIT.md
- CACHE_AUDIT.md
- TECH_DEBT.md
- DEVELOPMENT_GUIDE.md
- DECISIONS.md

### 理由

長期運用・保守・引き継ぎに耐えるプロジェクトにするため。

### 影響

- 機能追加時は関連ドキュメントを必ず更新する
- 判断理由を残す
- 設計と実装のズレを早期に発見できる
