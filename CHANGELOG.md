# CHANGELOG

## v1.0.23
- ホームだけヘッダーが崩れる問題を修正
- ホームヘッダーのりんちゃんアイコンを左、通信アイコンを右に固定し、中央の空白崩れを解消
- 通信ページ「ありがとうの広がり」で、本文と理由バッジが同じ内容で重複して見える問題を修正
- 通信ページのありがとうカードは、上段に部署間、下段に「ありがとう」「『助けてもらった』のありがとう」のみを表示する簡潔な形式に変更
- マイページのありがとう記録を「送信／受信」が一目で分かる色付き丸ラベル形式へ変更
- マイページのありがとう記録を「送信/受信＋相手名」「内容＋日付」の2行構成へ変更
- ホーム、通信、マイページのキャッシュを `1023` に更新

## v1.0.22
- ヘッダーの高さ、戻るボタン、右側アイコン、ページタイトルのサイズを共通CSSで統一
- ホーム、杜、歩数記録、通信、マイページのキャッシュを `1022` に更新
- 通信ページ「ありがとうの広がり」の本文を、部署名の重複を避けて「ありがとう」「『助けてもらった』のありがとう」のような短文に修正
- 通信ページのありがとう表示を最新5件相当に抑え、お知らせ・グループニュースは各3件表示に整理
- マイページの「最近の活動」は3件表示、「ありがとうの記録」は5件表示に整理
- 各リストに「最近N件を表示しています。」の補足を追加

## v1.0.21
- 杜ページのありがとう送信を、送信前に理由を選べる下部シート形式へ変更
- ありがとう理由として「ありがとう」「助けてもらった」「声をかけてもらった」「一緒にがんばった」を選択可能に変更
- マイページのありがとう一覧をカード式に整え、文字だけの羅列にならないよう修正
- 歩数記録ページの戻る矢印を他ページと同じ太さ・見え方に統一
- 通信ページ「今日の杜だより」の見出しを少し軽くし、他ページの見出しに近づけるよう再調整
- 杜、通信、マイページ、歩数記録のキャッシュを `1021` に更新

## v1.0.20
- 通信ページのありがとうカードを端末ローカルではなくサーバー側の公開タイムラインから直接取得するように修正
- 通信ページのありがとうカードを「日付＋部署間＋理由」で集約し、ユーザーごとにバラバラ表示されにくい形へ変更
- 通信ページで `window.RinchanNews` を公開し、ページ表示時の同期・再描画が確実に動くように修正
- マイページで `window.RinchanMypage` を公開し、同期後の再描画が確実に動くように修正
- マイページのありがとう欄を「ありがとうの記録」に変更し、送ったありがとう・もらったありがとうの両方を表示
- 通信ページ「今日の杜だより」の見出しフォントを他ページの見出しに合わせるよう調整
- 通信ページ、マイページのキャッシュを `1020` に更新

## v1.0.19
- 杜ページのありがとう送信中表示を、ボタン表示と画面下トーストで分かりやすく修正
- ありがとう送信成功後は対象者のボタンを「送信済み」に置き換え、連続送信・二重送信を防止
- ありがとう送信成功時にローカルの通信タイムライン、送信履歴、ありがとう件数を即時更新
- 通信ページ、マイページのキャッシュを `1019` に更新し、表示時に同期・再描画するよう修正
- ボタンの送信中・送信済み状態のCSSを追加

## v1.0.16
- Apps Script `saveThanks` のAPI処理を堅牢化
- ありがとう保存後のキャッシュ削除、ログ、監査ログ、状態取得でエラーが起きても保存自体は成功扱いになるように修正
- ありがとう保存に失敗した場合は、Apps Script側でエラー内容を返すように修正
- Apps Script の `VERSION` を `v1.0.16` に更新

## v1.0.15
- 通信ページ「今日の杜だより」の背景装飾を削除し、自然なグラデーション背景に修正
- 通信ページ上部の説明文を短くし、不自然な改行を防止
- ありがとうカードの表示元をサーバー側の公開タイムライン優先に変更
- ありがとうカードの表示名を個人名ではなく部署名ベースに修正
- ありがとう送信失敗時にローカルカードが増えないよう、杜ページと予備の送信ページを直接API送信方式に変更
- ホーム、杜、通信、ありがとうページのキャッシュバージョンを `1015` に更新

## v1.0.14
- ホーム「あなたの木」の説明文が右にはみ出る問題を修正
- 通信ページ「今日の杜だより」の説明文が右にはみ出る問題を修正
- 杜ページで自分自身の行には「ありがとう」ボタンを表示せず、「自分」表示に変更
- 杜ページの「杜の見どころ」上の余白を削減
- ありがとう表示をカード式に固定するため、通信ページの読み込み順とCSSを修正
- 杜ページのありがとうボタンは存在しない `thanks.html` へ遷移せず、その場で送信するように変更
- 旧キャッシュや古い遷移が残った場合に備え、`pages/thanks.html` を追加
- `css/v1014-hotfix.css` を追加し、主要画面のキャッシュバージョンを `1014` に更新

## v1.0.13
- Apps Script `getUserState` に全員分の `allActivities`、`moriMembers`、`departments`、`mori`、`forestSummary` を追加
- 杜の成長を本人歩数ではなく全員分の累計歩数で計算するように修正
- 部署マップを自部署だけでなく全部署・全メンバー集計から表示するように修正
- 通信ページの「今日のまとめ」を全員分の今日の記録・今日の歩数・累計歩数で表示するように修正
- ありがとう一覧をカード形式に修正
- ホーム「あなたの木」の成長メモを3行に分け、改行崩れを修正
- パソコン表示で「今週の歩数」の「歩」が右にズレる問題を修正
- `css/v1013-hotfix.css` を追加し、該当ページで読み込み
- 主要画面のキャッシュバージョンを `1013` に更新
