# CHANGELOG

## v1.0.13
- Apps Script `getUserState` に全員分の `allActivities`、`moriMembers`、`departments`、`mori`、`forestSummary` を追加
- 杜の成長を本人歩数ではなく全員分の累計歩数で計算するように修正
- 部署マップを自部署だけでなく全部署・全メンバー集計から表示するように修正
- 通信ページの「今日のまとめ」を全員分の今日の記録・今日の歩数・累計歩数で表示するように修正
- ありがとう一覧をカード形式で表示するように修正
- ホーム「あなたの木」の成長メモを3行に分け、改行崩れを修正
- パソコン表示で「今週の歩数」の「歩」が右にズレる問題を修正
- `css/v1013-hotfix.css` を追加し、該当ページで読み込み
- 主要画面のキャッシュバージョンを `1013` に更新

## v0.9.62
- `features/growth.js` を追加し、木の成長演出、季節表示、バッジ演出を移行
- `features/chart.js` を追加し、週間歩数グラフ、週目標、先週比較を移行
- `features/guide.js` を追加し、りんちゃん初回ガイドを移行
- `features/diagnostics.js` と `pages/diagnostics.html` を追加し、端末診断画面を実装
- `core/error-log.js` を追加し、端末内JavaScriptエラーログ保存に対応
- ホーム、ログイン、初回登録、歩数記録、通信、杜、マイページ、管理画面、診断画面で `error-log.js` を読み込み
- 診断画面でモジュール読込状態、API設定、キャッシュ件数、未送信キュー、syncToken、オンライン状態、端末ID、保存キー、エラーログを表示
- 管理画面に診断画面への導線を追加
- `index.html` の旧JS読み込みを `features/` 構成へ切り替え
- 旧 `v***.js` 主要ファイルを削除
- 全主要画面のキャッシュバージョンを `0962` に統一
- リポジトリ検索で旧JS名と旧キャッシュ参照が残っていないことを確認
- READMEをv0.9.62へ更新

## v0.9.61
- JavaScript を `core/` と `features/` の役割別構成へ移行
- `core/storage.js`、`core/api.js`、`core/sync.js`、`core/offline-queue.js` を追加
- `features/auth.js`、`features/activity.js`、`features/thanks.js`、`features/news.js`、`features/mori.js`、`features/mypage.js`、`features/admin.js` を追加
- ホーム、ログイン、初回登録、歩数記録、マイページ、通信、杜、管理画面を新構成へ切り替え
- 主要画面のキャッシュバージョンを `0961` に統一
- READMEをv0.9.61へ更新

## v0.9.60
- `v160-offline-queue.js` を追加
- 通信失敗時に歩数保存、歩数削除、ありがとう送信、プロフィール保存、通信既読を未送信キューへ保存
- 通信復帰時、画面復帰時、手動再送時に未送信データを再送
- ホーム、通信、歩数記録、杜、マイページ、ログイン、初回登録、管理画面にオフライン再送キューを組み込み

## v0.9.59
- `getUserState` に `syncToken` を追加
- クライアントから `syncToken` を送信して差分同期できるように対応
- サーバー側で変更なしの場合は `unchanged: true` の軽量レスポンスを返すように変更
- `v135-sync.js` で `syncToken` を保存・送信するように変更
- 変更なしの場合は画面再描画を省略し、体感速度と通信量を改善

## v0.9.58
- 保存系APIが最新 `state` を返すように変更
- `v135-sync.js` を強化し、保存後の最新状態を即反映
- `v158-state-bridge.js` を追加し、API応答から最新状態を自動反映
- ホーム、ログイン、初回登録、歩数記録、通信、杜、マイページ、管理画面を v0.9.58 に更新
- READMEをv0.9.58へ更新

## v0.9.56
- Apps Scriptを機能別ファイル構成へ分割
- `Code.gs` をAPI入口専用に整理
- `Config.gs`、`Common.gs`、`Setup.gs`、`User.gs`、`Activity.gs`、`Thanks.gs`、`News.gs`、`Admin.gs` を追加
- 同期エンジンを「即表示＋裏同期」方式へ整理
- 画面下に最終同期表示を追加
- 同期中・同期済み・通信失敗の状態表示を追加
- どの端末でログインしてもスプレッドシート側から本人データを復元する方針に統一
- READMEをv0.9.56へ更新

## v0.9.49
- 通信ページの「ありがとうの広がり」と「最近届いたありがとう」を1つのカードに統合
- 上部に「最新のありがとう」を代表表示
- 下部に「最近のありがとう」を一覧表示
- 一覧に「助けてもらった」などの種類ラベルを表示
- 通信ページのキャッシュバージョンを `v0949` に更新
- `pages/news.html`、`js/v051-news.js`、`css/v100-news-ui.css` を更新
- READMEをv0.9.49へ更新

## v0.9.34
- 全ページ共通の下部メニューバー余白を再調整
- メニュー下に出ていた広い白い空白を削減
- ホーム・杜・歩数記録・通信・マイページのキャッシュバージョンを `v0934` に更新
- `css/v128-mobile-polish.css` を更新
- READMEをv0.9.34へ更新

## v0.9.32
- 杜ページの部署カード配置を調整し、カード同士の重なりを解消
- 長い部署名を自然に改行する表示へ変更
- 部署詳細カードの注意文を読みやすい2行表示へ変更
- 登録メンバーなしの文言の改行バランスを調整
- 杜ページのキャッシュバージョンを `v0932` に更新
- `css/v113-mori-groups.css` を更新
- `js/v132-mori-polish.js` を追加
- READMEをv0.9.32へ更新
