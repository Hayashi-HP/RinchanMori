# CHANGELOG

## v1.0.31
- 通信ページから「未読のお知らせ」セクションを削除
- 通信ページの通知描画処理を空処理にし、残っている旧要素があっても表示されないように修正
- マイバッジを丸いメダル風バッジデザインに変更
- 未獲得バッジの鍵マークを、メダル外側に重ねても欠けないSVG＋レイヤー構成に変更
- バッジ表示専用CSS `css/v1031-badge-polish.css` を追加
- 通信ページ、マイページのキャッシュを `1031` に更新

## v1.0.30
- 動作診断で `core/storage.js`、`core/error-log.js`、`core/sync.js`、`core/offline-queue.js`、`API URL` が未読み込み・未設定と表示される問題を修正
- `js/config.js`、`js/core/storage.js`、`js/core/error-log.js`、`js/core/sync.js`、`js/core/offline-queue.js` を `window` に公開するように修正
- 管理画面、診断画面で最新コアJSを読み込むようにキャッシュを `1030` に更新
- カプセル型ボタン統一用CSS `css/v1030-capsule-buttons.css` を追加
- 管理画面、診断画面の「動作診断を開く」「管理画面へ戻る」「マイページへ戻る」「消去」などのボタンをカプセル型に統一
- 診断結果と端末エラーログの行表示をスマホで読みやすいカード型に調整

## v1.0.29
- 旧導線用の `pages/thanks.html` を他ページと同じヘッダー、横幅、左右余白に統一
- `pages/thanks.html` で共通CSS `v1014-hotfix.css` を最新キャッシュで読み込むように変更
- ありがとう送信ページの説明文と選択欄を整理し、4種類から選ぶことが分かる表示へ調整
- `pages/thanks.html` のキャッシュを `1029` に更新

## v1.0.28
- `diagnostics.html` のヘッダーを他ページと同じ高さ・戻るボタン・右側更新アイコンに統一
- 診断ページのヒーロー、集計枠、診断結果、端末エラーログ、端末情報、戻るボタンの横幅を他ページと同じ最大390pxに統一
- 診断ページで共通CSS `v1014-hotfix.css` と管理系調整CSS `v1026-admin-mypage.css` を読み込むように変更
- 診断ページのキャッシュを `1028` に更新

## v1.0.27
- マイページのログアウトエリアから大きな見出し「ログアウト」を削除
- ログアウトエリアを「アカウント」「この端末の利用を終了します。」「ログアウト」ボタンだけの簡潔な表示に変更
- マイページのキャッシュを `1027` に更新

## v1.0.26
- 管理者設定のあるユーザーだけ、マイページのヘッダー右側に管理画面リンクアイコンを表示
- 管理画面リンクアイコンから `admin.html` へ遷移できるように変更
