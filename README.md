# RinchanMori v1.0.15

みんなで育てる、健康と笑顔の杜。

## v1.0.15 内容

- 通信ページ「今日の杜だより」の背景装飾を削除し、自然なグラデーション背景に修正
- 通信ページ上部の説明文を短くし、不自然な改行を防止
- ありがとうカードの表示元をサーバー側の公開タイムライン優先に変更
- ありがとうカードの表示名を個人名ではなく部署名ベースに修正
- ありがとう送信失敗時にローカルカードが増えないよう、杜ページと予備の送信ページを直接API送信方式に変更
- ホーム、杜、通信、ありがとうページのキャッシュバージョンを `1015` に更新

## v1.0.14 内容

- ホーム「あなたの木」の説明文が右にはみ出る問題を修正
- 通信ページ「今日の杜だより」の説明文が右にはみ出る問題を修正
- 杜ページで自分自身の行には「ありがとう」ボタンを表示せず、「自分」表示に変更
- 杜ページの「杜の見どころ」上の余白を削減
- ありがとう表示をカード式に固定するため、通信ページの読み込み順とCSSを修正
- 杜ページのありがとうボタンは存在しない `thanks.html` へ遷移せず、その場で送信するように変更
- 旧キャッシュや古い遷移が残った場合に備え、`pages/thanks.html` を追加
- `css/v1014-hotfix.css` を追加
- ホーム、杜、通信ページのキャッシュバージョンを `1014` に更新

## v1.0.13 内容

- 杜の成長を全員分の累計歩数で計算するように修正
- 部署マップを全メンバー・全部署の集計で表示するように修正
- 通信ページ「今日のまとめ」を本人分ではなく全員分の今日の記録・今日の歩数・累計歩数で表示
- ありがとうの広がりをカード形式に修正
- ホーム「あなたの木」の成長メモの改行崩れを修正
- パソコン表示で「今週の歩数」の「歩」が右にズレる問題を修正
- Apps Script の `getUserState` が `allActivities`、`moriMembers`、`departments`、`mori`、`forestSummary` を返すように拡張
- `css/v1013-hotfix.css` を追加
- ホーム、杜、通信ページのキャッシュバージョンを `1013` に更新

## JavaScript 構成

```text
js/
├── core/
│   ├── storage.js          # localStorage、JSON安全処理、端末ID、ユーザーデータ管理
│   ├── error-log.js        # 端末内JavaScriptエラーログ保存
│   ├── api.js              # Apps Script API 通信
│   ├── sync.js             # 差分同期、同期状態表示、サーバー状態反映
│   └── offline-queue.js    # 通信失敗時の未送信保存、自動再送
│
└── features/
    ├── auth.js             # ログイン、初回登録、ログアウト、プロフィール保存
    ├── activity.js         # 歩数保存、編集、削除、最近の記録
    ├── thanks.js           # ありがとう送信、受信、集計、タイムライン
    ├── news.js             # 通信、お知らせ、既読、通知バッジ
    ├── mori.js             # 杜、部署、木、杜レベル
    ├── mypage.js           # マイページ、バッジ、継続、目標表示
    ├── admin.js            # 管理画面、集計、利用者一覧
    ├── diagnostics.js      # 動作診断、端末情報、エラーログ表示
    ├── growth.js           # 木の成長演出、季節表示、バッジ演出
    ├── chart.js            # 週間歩数グラフ、週目標、先週比較
    └── guide.js            # 初回ガイド、チュートリアル
```

## Apps Script 構成

Google Apps Script 側にも、以下のファイル名で分けて作成してください。

```text
apps-script/
├── Code.gs       # API入口 doGet / doPost
├── Config.gs     # 定数・初期部署
├── Common.gs     # 共通関数
├── Setup.gs      # シート初期化・部署マスタ
├── User.gs       # ログイン・プロフィール・本人状態・全体杜状態
├── Activity.gs   # 歩数登録・削除・履歴
├── Thanks.gs     # ありがとう保存・受信・送信・タイムライン
├── News.gs       # 通信既読
└── Admin.gs      # 管理画面・ランキング集計
```

## Apps Script 反映手順

1. Google スプレッドシートを開く
2. 拡張機能 → Apps Script を開く
3. GitHub の `apps-script/` 配下の同名ファイルで Apps Script 側を置き換える
4. `setupProjectManual()` を一度実行
5. Web アプリを再デプロイ

## 今回の確認項目

- 今日の杜だよりの背景が白い雲状に抜けず、自然な背景で表示されること
- 今日の杜だよりの文章が不自然な位置で改行されないこと
- ありがとう送信に失敗してもカードが増えないこと
- ありがとうカードの見出しが個人名ではなく部署名になること
- 杜ページからありがとう送信後、成功時だけカードが増えること

## 同期方針

りんちゃんの杜では、正式データはスプレッドシートを正とします。

通常利用では、スマホ内のデータをキャッシュとして使い、画面はすぐ表示します。その後、裏側で Apps Script に接続し、スプレッドシート側の最新データで画面を更新します。

```text
アプリを開く
↓
手元キャッシュで即表示
↓
裏でサーバー同期
↓
最新データで必要箇所だけ更新
```

保存系APIは、保存が成功したあと `getUserState` 相当の最新状態を返します。画面側はその `state` を受け取り、以下をまとめて更新します。

- プロフィール
- 本人の歩数履歴
- 全員分の歩数履歴
- 杜メンバー
- 部署一覧
- 杜全体の累計歩数
- 通信ページの全体サマリー
- 受け取ったありがとう
- 送ったありがとう
- 通信タイムライン
- お知らせ既読状態
- ありがとう件数

ログアウト後に再ログインした場合も、`loginUser` と `getUserState` により同じ内容を復元します。
